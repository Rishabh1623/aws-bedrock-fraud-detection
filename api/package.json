{
  "name": "fraud-detection-api",
  "version": "1.0.0",
  "description": "Real-time fraud detection API",
  "scripts": {
    "deploy": "sam build && sam deploy --guided",
    "test": "jest"
  },
  "dependencies": {
    "aws-sdk": "^2.1500.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "aws-sam-cli": "^1.100.0"
  }
}
